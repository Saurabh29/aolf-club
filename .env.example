# Google Maps API Key for Places Autocomplete
# Get one from: https://console.cloud.google.com/google/maps-apis/credentials
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# DynamoDB Configuration
# Table name for all entities (single-table design)
DYNAMODB_TABLE_NAME=aolfclub-entities

# AWS Region
AWS_REGION=us-east-1

# DynamoDB Endpoint (set for local development with DynamoDB Local)
# When set, the client uses local credentials { accessKeyId: "local", secretAccessKey: "local" }
# Comment out or remove for production (uses default AWS credential chain)
DYNAMODB_ENDPOINT=http://localhost:8000

# For DynamoDB Local, these can be any non-empty strings
# For production, remove these and use AWS credential chain (IAM roles, env vars, etc.)
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local
